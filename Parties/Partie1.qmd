---
title: "Partie 1"
format: pdf
---

\- faire les statitiques finales sur la dernière variables Y et verrifier les atypiques avant et après la sationnarisation

```{r}
library(stR)
```

```{r}
library(tsoutliers)
y <- read.csv2("Base/base_complete.csv")
getwd()
yy <- ts(data = y, start=c(1999,01),frequency=12)
```
